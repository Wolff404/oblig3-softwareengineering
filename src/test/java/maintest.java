/*
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

    public class maintest {
        @Test
        public static boolean isLeapyear(int year) {
            if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
                return true;
            }
            return false;

        }

        @Test
        public void testLeapyear () {
            assertTrue(isLeapyear(2024));
            assertTrue(isLeapyear(2000));
            assertFalse(isLeapyear(1900));
            assertFalse(isLeapyear(2100));
        }

        @Test
        public static boolean isLeapyearDivisableBy4ButNot100(int year) {
            if (year % 4 == 0 && year % 100 != 0) {
                return true;
            }
            return false;
        }

        @Test
        public static boolean isLeapyearDivisableBy400(int year) {
            if (year % 400 == 0) {
                return true;
            }
            return false;
        }

        @Test
        public static boolean yearNotDivisiableBy4(int year) {
            if (year % 4 != 0) {
                return true;
            }
            return false;
        }

        @Test
        public static boolean yearDivisableBy100ButNot400(int year) {
            if (year % 100 == 0 && year % 400 != 0  ) {
                return true;
            }
            return false;
        }


    }



*/